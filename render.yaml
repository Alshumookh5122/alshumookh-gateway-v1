services:
  - type: web
    name: alshumookh-api
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.app.main:app --host 0.0.0.0 --port 10000"
